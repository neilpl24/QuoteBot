{
  "name": "discordbot",
  "version": "1.0.0",
  "description": "This bot makes commands for a message that the user chooses to pick.",
  "main": "main.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Neil Pierre-Louis",
  "license": "ISC",
  "dependencies": {
    "discord.js": "^12.3.1"
  }
}
